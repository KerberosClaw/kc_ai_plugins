# QA 報告：<對象> <輪次>

> 本檔是空白骨架。填寫規則見 `references/report-contract.md`，出貨前跑完那份的九條自檢。

## 一、摘要

**整體狀態**：`尚未實跑` ／ `部分執行` ／ `已執行，有未結案缺陷` ／ `已執行，本輪範圍內無未結案缺陷`

**證據限定詞**：<N 條案例中執行 M 條，X 條因為 ⋯⋯ 未執行；本輪只涵蓋 ⋯⋯，未涵蓋 ⋯⋯>

**本輪涵蓋**：

**本輪不涵蓋**：

**最需要注意的三件事**：

1.
2.
3.

## 二、環境與可重現性

| 項目 | 內容 |
|---|---|
| 環境 | |
| 執行時間 | |
| 使用帳號與角色 | |

**三方版本座標**（階段 0）：

| | 版本（提交、標籤、映像或建置時間） |
|---|---|
| 需求文件 | |
| 原始碼 | |
| 部署中的系統 | |

<對不上的地方與受影響範圍；對得上就寫「三方一致」>

**材料清單**（階段 0，照 `SKILL.md` 的表原樣貼上）：

**前提盤點**（現成／可以造出來／造不出來／還沒確認但先當成立）：

**前提重建方式**：<重建得出來的寫做法，重建不出來的寫為什麼>

## 三、測試矩陣

| 案例 | <前提 A> | <前提 B> | <前提 C> | <前提 D> |
|---|---|---|---|---|
| `TC-01` <簡述> | | | | |
| `TC-02` <簡述> | | | | |

狀態：`PASS` ／ `FAIL` ／ `UNCLEAR` ／ `BLOCKED` ／ `NOT RUN`

**空白格說明**（這條案例在這個前提下沒有意義，以及為什麼）：

**`NOT RUN` 說明**（有意義但沒跑，原因）：

## 四、發現

### 4.0 本輪修正

> 只有多輪時才需要。對帳結果是「已修好」的列在這裡，每條附前後兩輪的截圖對照。

| 編號 | 前一輪狀態 | 本輪證據 |
|---|---|---|

### 4.1 沒有人決定過的（`UND`）

系統現在就這樣做，但需求文件裡找不到有人決定過。**這些不是缺陷，是待判定。**

| ID | 系統實際要求什麼 | 來源與證據 | 影響 | 要問誰 |
|---|---|---|---|---|
| `UND-01` | | | | |

### 4.2 缺陷

<每條照 `references/report-contract.md`「缺陷條目怎麼寫」的四件事展開>

#### `TC-##` <一句話：哪裡壞、怎麼壞>

**嚴重度**：P<N>。<誰會碰到、多常碰到、繞不繞得過去>（多輪時照對帳表並排寫各份的原值，例如 `R1:P1 / R2:P3`）

**追溯**：`TC-##` → `EC-##`／`UND-##`／`GAP-##`／`MIS-##`

**怎麼再看到一次**：<環境、角色、前提>；<步驟 1、2、3⋯>；<試 N 次出現 M 次>

**實際看到**：<描述>

![<圖說>](img/<檔名>.png)
<這張圖證明了什麼>

<網路訊號、主控台訊號>

**該看到**（`依需求`／`推定`）：<預期；推定要附依據>

**還判斷不了的**：<沒有就寫「無」>

### 4.3 漏與錯位（`GAP`、`MIS`）

| ID | 類型 | 需求說什麼 | 實作做什麼 | 影響 |
|---|---|---|---|---|

### 4.4 邊界與前提（`EC`）

| ID | 面向 | 誰會踩到 | 系統實際怎麼做 | 證據狀態 | 轉成哪些案例 |
|---|---|---|---|---|---|

## 五、分歧

> 🔴 這一節永遠要有。沒有衝突就明寫「無」，不要整節刪掉。

<照對帳表裡「說法衝突」的那幾列填，兩邊原樣並排>

## 六、證據邊界

**已驗證**：

**未驗證**：

**無法驗證，以及缺什麼**：

## 七、本輪寫入動作

> 唯讀就明寫「本輪未寫入任何資料」。

| 時間 | 端點或操作 | 寫了什麼 | 留下什麼資料 |
|---|---|---|---|

## 八、待答問題與下一步

| 問題 | 要誰回答 | 什麼證據才關得掉 |
|---|---|---|

---

## 手冊素材交棒

> 本節給 `project-docs` 用，不是 QA 報告的一部分。

**成功路徑截圖**：`<輪次>/manual/` 底下 N 張，涵蓋 <哪幾條路徑>

**可以寫進手冊「使用前提」的隱含要求**：<列出 `UND` 與 `EC` 裡屬於合理設計、只是沒寫出來的那些>

**可以寫進手冊「常見問題」的卡關點**：<列出 `UNCLEAR` 那些>

---

## 附錄：對帳表

> 只有多輪或多視角時才需要。做法見 `references/merge-rules.md`。

| 編號 | R1 | R2 | 對帳結果 |
|---|---|---|---|
